apiVersion: v1
kind: Service
metadata:
  name: {{ include "horizons.fullname" . }}
  labels:
    {{- include "horizons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "horizons.selectorLabels" . | nindent 4 }}
